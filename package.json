{
  "name": "mern-biolerplate-server",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "start": "nodemon ./bin/www",
    "setup": "npm install && cd client && npm install && npm run-script build && echo Setup complete!"
  },
  "dependencies": {
    "body-parser": "^1.19.0",
    "cors": "^2.8.5",
    "debug": "~2.6.9",
    "express": "~4.16.1",
    "http-errors": "~1.6.3",
    "mongodb": "^3.6.7"
  },
  "devDependencies": {
    "nodemon": "^2.0.7"
  }
}
